========================================================================
    IMPORT LIBRARY: newdev Project Overview
========================================================================

This is an import library for newdev.dll which supplies to the Installer a few
import names that would have to be delay-linked otherwise.